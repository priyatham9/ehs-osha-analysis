# Runtime dependencies. Deliberately minimal: the analysis runs on Python 3.9
# with pandas, numpy and the standard library, and nothing else.
#
# There is no SciPy (optimisers and the chi-square tail are implemented in
# src/ehs_osha/optimize.py and src/ehs_osha/countmodels.py), no matplotlib
# (figures are written as SVG by src/ehs_osha/svgplot.py), and no test runner
# beyond stdlib unittest.
#
# Verified against pandas 2.3.3 / numpy 2.0.2 on CPython 3.9.6.
pandas>=1.3
numpy>=1.20
